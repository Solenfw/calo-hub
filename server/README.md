# Calo Hub Backend

FastAPI backend for Calo Hub application.

## Setup

1. Install dependencies: `uv sync`
2. Run development server: `uv run uvicorn app.main:app --reload`

## Features

- User authentication with JWT
- Product catalog management
- CSV import functionality
- PostgreSQL database