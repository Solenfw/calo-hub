
export interface KLSProduct {
    code: string;
    viet_desc: string;
    eng_desc: string;
}

export interface AesculapProduct {
    code: string;
    viet_desc: string;
    eng_desc: string;
    image: string;
    alternative_code: string;
}