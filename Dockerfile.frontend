# Dockerfile for Next.js frontend with Bun
FROM oven/bun:1.0 AS base
WORKDIR /app/frontend

COPY frontend/package.json frontend/bun.lock ./
RUN bun install --frozen-lockfile

COPY frontend .
RUN bun run build

EXPOSE 3000
CMD ["bun", "run", "start"]
