# Dockerfile for FastAPI backend with Bun
FROM oven/bun:1.0 AS base
WORKDIR /app/backend

COPY backend/package.json backend/bun.lock ./
RUN bun install --frozen-lockfile

COPY backend .

EXPOSE 8000
CMD ["bun", "run", "dev"]
